name: User Validation Action

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 23f3003492@ds.study.iitm.ac.in
        run: |
          echo "Validation step for 23f3003492@ds.study.iitm.ac.in"
          echo "Action triggered successfully."
